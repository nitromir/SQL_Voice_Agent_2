export class AudioProcessor {
    // Пример метода обработки аудио
    public processAudioData(audioData: Int16Array): void {
        console.log('Processing audio data:', audioData);
        // Здесь можно добавить логику обработки аудиоданных
    }
} 